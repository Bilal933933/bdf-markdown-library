# Foundation فقط — لا تعلن متغيرًا لا يستخدمه الأساس.
# (DATABASE_URL / REDIS_URL / STORAGE_DIR / GEMINI_* تأتي مع مراحلها)
APP_NAME=document-conversion-engine
APP_ENV=local
DEBUG=true
LOG_FILE=logs/app.log
LOG_MAX_BYTES=10485760
LOG_BACKUP_COUNT=5
