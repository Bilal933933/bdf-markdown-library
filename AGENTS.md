# دليل الوكيل: معالجة ملفات PDF للكتب العربية

هذا الدليل موجّه للوكيل (الذكاء الاصطناعي) عند استلام أي ملف PDF كتاب عربي.
الهدف: تحويل أي PDF إلى **ملفات Markdown نصية نظيفة** بخطوات موحّدة.

---

## 1) استقبال الملف

- عند رفع ملف PDF، لا تحاول قراءته مباشرة (النموذج لا يدعم عرض PDF).
- سجّل المسار المعطى، ثم نفّذ خطوات الفحص التالية.

## 2) تحديد طريقة الاستخراج (فحص تلقائي)

اعرض مسارًا على الأدوات ولا تفترض. خطوات الفحص:

1. **تأكيد وجود الملف وحجمه** (سياق الاستخدام: PowerShell).
2. **عدد الصفحات وفحص الطبقة النصية**: افتح الـPDF بـ `pdfjs-dist` واقرأ `numPages`، ثم اقرأ `getTextContent()` لأول 3 صفحات:
   - إذا كان مجموع النص في الصفحات الثلاث الأولى **أكبر من 1000 حرف** → الملف **له طبقة نصية** → **استخراج مباشر**.
   - إذا كان النص **قليلًا أو صفريًا** (فقط فواصل صفحات / رموز تحكم) → الملف **ممسوح ضوئيًا** → **الاستخراج بالـOCR**.

### 2أ) استخراج مباشر (له طبقة نصية)
- استخدم: `node _ocr/extract-text.mjs "<اسم الكتاب>" "<مسار الملف>" <عدد الصفحات>`
- الناتج: مجلد `<اسم الكتاب>/` يحتوي `part-01.md` ... كل ملف 40 صفحة، بصيغة:
  - سطر ترويسة: `# <اسم الكتاب> — الجزء N (صفحات a-b)`
  - كل صفحة: `## صفحة <رقم>\n\n<النص>`

### 2ب) استخراج بالرؤية الذكية (Gemini Vision) — المسار الأساسي للكتب الممسوحة
- استخدم السكربت `gemini-ocr.mjs` (يعرض الصفحة كصورة لنموذج رؤية Gemini ويستخرج النص):
  `node _ocr/gemini-ocr.mjs "<مسار PDF>" "<مجلد الناتج>" [startPage] [endPage]`
- متغيرات بيئة:
  - `GEMINI_API_KEY`: المفتاح (يفضّل تمريره عبر البيئة دون كتابته في ملفات المستودع).
  - `GEMINI_MODEL`: النموذج الأساسي (المجرّب: `gemini-3.1-flash-lite`).
  - `GEMINI_FALLBACK`: قائمة نماذج احتياطية مفصولة بفواصل (يدور السكربت بينها تلقائيًا).
  - `GEMINI_PROMPT`: نص طلب الاستخراج (الافتراضي المثبّت: `انسخ محتوى هذه الصفحة نصًا فقط، لا شيء آخر.`).
  - `NO_RESUME=1`: تجاهل `progress.json` والبدء من الصفحة المحددة.
- **الـPROMPT قصير دائمًا**: أي توجيه أطول يُفعّل فحص `RECITATION` فيرفض النموذج صفحات الآيات القرآنية (أسقطت صفحة 257 قبل تبسيط الـPROMPT).
- **الحصص منفصلة لكل نموذج وتنفد سريعًا** (~16-80 صفحة/جلسة) → يعيد السكربت المحاولة عبر كل النماذج ثم ينتظر 60 ثانية عند استنفادها جميعًا (حد 12 محاولة/صفحة).
- **دمج آمن**: عند تشغيل نطاق جزئي يدمج النتائج مع الأجزاء الموجودة (لا يكتب فوقها) ويكتب `progress.json` للاستئناف.
- الزمن المتوقع: ~15-25 ثانية/صفحة. كتاب 550 صفحة قد يتجاوز ساعتين مع دورة الحصص — لا تقطع وأبلغ بالزمن المتوقع.

#### آلية التبديل التلقائي بين النماذج عند نفاد الحصص (مدمجة في `gemini-ocr.mjs`)

السكربت لا يتوقف عند نفاد حصة نموذج واحد، بل **يدور تلقائيًا عبر كل النماذج المتاحة** بالترتيب: `GEMINI_MODEL` أولًا ثم قائمة `GEMINI_FALLBACK` (مفصولة بفواصل). المبدأ العملي:

1. **لكل صفحة، تُجرّب النماذج واحدًا تلو الآخر** بالترتيب (النموذج الأساسي ثم البدائل). أول نموذج يعيد نصًا غير فارغ يُعتمد.
2. **تصنيف الخطأ**:
   - `HTTP 429` أو `503` (استنفاد حصة/ضغط مرتفع) → يُعتبر النموذج **نافد الحصة** حاليًا ويُتخطى لهذه الصفحة مع مواصلة تجربة البقية.
   - أي خطأ آخر (مهلة/شبكة) → يعالج كفشل حقيقي لهذا النموذج.
3. **خوارزمية الدوران**: حلقة `for t in 0..11` لكل صفحة؛ في كل دورة تُجرّب كل النماذج. إذا كانت **كل النماذج** نافدة الحصة (429/503) → يصبر 60 ثانية ثم يعيد المحاولة (حتى 12 دورة ≈ عدة دقائق انتظار). إذا ظهر خطأ غير حصّي حقيقي → يتوقف فورًا عن الدورات الزائدة.
4. **النتيجة**: نموذج قد ينجح 40 صفحة ثم يُستنفد بينما غيره ما زال متاحًا — المعالجة تستمر **بدون تدخل يدوي** ولا توقف عند إشعار حصة.
5. **تخصيص القائمة**: غيّر `GEMINI_MODEL` إلى النموذج الأقوى/الأدق المتاح حاليًا، وضع الباقي في `GEMINI_FALLBACK`. للتصعيد أضف النماذج الجديدة (تحقّق من قابليتها عبر `models.list()` أو فحص سريع) دون تغيير منطق السكربت.

#### بديل قديم (Tesseract) — لا يُفضَّل للعربية المشكّلة
- `_ocr/ocr-book-jpx.mjs` (JPEG2000 + Tesseract `ara_best`) يُستخدم فقط عند انقطاع الوصول لنماذج الرؤية.
- دقته للعربية المشكّلة 65-75% تقريبًا ولا يُناسب كتب النحو/البلاغة/الصرف حيث التشكيل جوهر المحتوى (لكنه يتعامل جيدًا مع JPEG2000 والصيغ العادية عبر `wasmUrl`).

## 3) مرحلة المراجعة والتصحيح (إلزامية بعد أي استخراج)

بعد توليد الملفات، يجب تمريرها على **مراجعة** تلقائية:

### 3أ) فحص هيكلي
- عدد الملفات والملفات المدخلة بصيغة `part-XX.md`.
- عدد صفحات فعلية: عدّ أسطر `^## صفحة` في كل ملف.
- **صفحات فاشلة**: مضمون `[تعذر استخراج النص من الصفحة N]` → أعد معالجتها عبر `gemini-ocr.mjs`.
- **صفحات فارغة**: صفحة `## صفحة N\n\n` بلا محتوى وليست بمحتوى فني أصلي.
- تحقق أن عدد ملفات = ceil(الصفحات/40) وأن ترقيم الصفحات استمرار وحدة (لا فجوة).

### 3ب) فحص جودة النصوص
- **أحرف تحكم**: حوّل نص كل صفحة، فحص Regex `[\x00-\x1F\x7F-\x9F]` (مع علامة U+0003, U+0004, U+0010, ... ظهرت في المقرر). إذا وُجدت:
  - أزل الأحرف غير القانونية فقط (التنظيف)، أو
  - إذا كان النص **مفيدًا فعليًا < 80 حرف** بعد إزالة التحكم (محطم/ترويسات زخرفية) → **أعد الصفحة عبر `gemini-ocr.mjs`** من صورة الصفحة (أو سكربت المراجعة `_ocr/fix-pages.mjs` بمنطق: تنظيف + OCR للصفحات القصيرة).
- **صفحات نصها المختصر جدًا (<80 حرفًا مفيدًا)**: إما محتوى فني (رسوم/صور/فهارس مصورة — لا توجد وسيلة) أو محتوى محطم (تأكيد بالـOCR من الصورة).
- **صفحات بها عبارة "الإدارة المركزية لتطوير المناهج" تروسية مشوهة ترميزيًا**: خاص بحالات — الحل: OCR من صورة الصفحة.

### 3ب-ب) فحص جودة عميق بالعتبات (اختِصاري — عدّ لكل صفحة)
لكل صفحة في الملفات، احسب:
1. `l` = طول **النص المفيد**: إزالة أحرف التحكم `[\x00-\x1F\x7F-\x9F\u200B-\u200F\u202A-\u202E\uFEFF]` ثم إزالة الفراغات `\s+`.
2. `c` = **عدد أحرف التحكم** قبل الإزالة.

صنّف صفحة في إحدى الفئات:

| فئة | الشرط | المعالجة |
|-----|--------|-----------|
| A سليمة | `l ≥ 200` | إبقاء كما هو |
| B قصيرة مريبة | `80 ≤ l < 200` | اطبع نصها وصنّفه يدويًا: إن محتوى فني أبقِه؛ وإلا أعد OCR |
| C محطمة | `l < 80` **أو** محتوى فني أصلي فاشل | أعد الصفحة بالـOCR من صورة الصفحة (خفف `--psm` إلى الافتراضي إن كان `--psm 6` قد فشل) |
| D أحرف تحكم كثيرة | `c / (l + 1) > 0.3` | أزل التحكم ثم أعد تصنيفها بالمعايير أعلاه |

ملاحظات مستخلصة من سجلات سابقة (حالات حقيقية):
- صفحات محتوى فني (رسوم، غلافات وحدات، فهارس مصوّرة) تعطي `l` منخفضًا **بشكل طبيعي** — لا تعيد معالجتها بل أبقِ نصها الناتج مع تعليق `<!-- صفحة فنية -->`.
- صفحة غلاف بنص ترويسة (مثل "الإدارة المركزية لتطوير المناهج") قد تحوي أحرف تحكم داخل الكلمات: عالج بالـOCR من الصورة، لا بالتنظيف وحده.
- بعد إعادة OCR صفحة، أعد قياس `l` الجديد: إن بقى `< 80` وكانت الصفحة ليست فنية → أبلِغ عنها في التقرير.
- عدد الصفحات المعاد معالجتها مؤشر جودة عام: ذكره في التقرير مع قائمة أرقامها.

### 3ج) التقرير النهائي
نفّذ وشغّل تقريرًا بالمنطق الآتي (إخراج للمستخدم):
`الملفات: N | إجمالي الصفحات: M | فاشلة: X | فارغة: Y`
ثم اختر أسماء الصفحات المعالجة بالـOCR ومحتواها إن أمكن.
أضف **مؤشر جودة**: `صفحات أعيدت بالـOCR: Z (الأرقام: ...) | صفحات فنية محفوظة: W`.

## 4) الأدوات الجاهزة (لا تنشئ سكربتة جديدة إلا للضرورة)

| السكربت | الاستخدام | متى |
|---------|-----------|-----|
| `_ocr/gemini-ocr.mjs` | OCR بالرؤية الذكية (Gemini Vision) نص مشكّل دقيق | المسار الأساسي للكتب الممسوحة |
| `_ocr/rebuild-parts.mjs <مصدر> <ناتج> [totalPages]` | دمج كل الصفحات الناجحة الفريدة وإعادة بناء الأجزاء مرتبة حسب رقم الصفحة مع بيان المفقودة | بعد اكتمال الاستخراج أو عند الحاجة لدمج نطاقات متعددة |
| `_ocr/ocr-book-jpx.mjs` | OCR قديم بـ Tesseract (JPEG2000) | احتياطي فقط عند انقطاع نماذج الرؤية |
| `_ocr/render-test.mjs` | اختبار صفحة وسطية (غرض: مطابقة الدقة/البياض) | قبل تشغيل OCR كامل |
| `_ocr/extract-text.mjs` | استخراج مباشر للطبقة النصية | عندما النص موجود (فوري) |
| `_ocr/fix-pages.mjs` | مراجعة: تنظيف + OCR للصفحات القصيرة/المحطمة | بعد أي استخراج |
| `_ocr/verify-book.mjs <مجلد>` | تقرير هيكلي (عدد الصفحات/فارغة/فاشلة) | في المراجعة |

## 5) استخلاصات عملية من معالجة "كيف تتقن النحو" (553 صفحة)

- **فحص `RECITATION`**: صفحات الآيات القرآنية (حالة الصفحة 257) يُرفضها النموذج إن طالت التوجيهات؛ العالج الناجح هو الـPROMPT القصير المثبّت.
- **الحصص منفصلة لكل نموذج**: نموذج قد ينجح 40 صفحة ثم يستنفد (429) بينما غيره متاح — اعتمد الدوران التلقائي ولا توقف المعالجة عند إشعار حصة.
- **الدمج الآمن**: عند تشغيل نطاق جزئي (صفحات مفقودة) يدمج السكربت مع الأجزاء الموجودة — لا تكرار ولا حذف. الاختبار اليدوي بهرب `\n` الحرفي عبر PowerShell → استخدم أداة التحرير أو سكربت Node عند إدراج نصوص يدويًا.
- **الصفحات الفنية قصيرة بطبيعتها**: صفحتا الغلاف (2: "كيف تتقن النحو؟") والبسملة (5: "بسم الله الرحمن الرحيم") <80 حرفًا — تحقق من المحتوى قبل اعتبارها فاشلة وأبقِها مع تعليق `<!-- صفحة فنية -->`.
- **توحيد الترويسة**: بعد إعادة البناء، وحّد كل جزء إلى `# <اسم الكتاب> — الجزء N (صفحات a-b)` لمطابقة معيار المستودع.
- **استخدام `rebuild-parts.mjs`**: بعد اكتمال كل الصفحات، أعد بناء الأجزاء من كل الصفحات الناجحة الفريدة لضمان ترتيب مستمر بلا فجوات.

## 6) قواعد صرفة
- **لا تقم بتغيير أو كتابة الملفات الأصلية** (PDF). فقط النتائج في مجلد جديد.
- استخدم مجلد `_ocr/` للأدوات والمجلدات الدائمة للنتائج.
- **العمل باللغتين**: أسماء الملفات والترويسات بالعربية، والتعليمات ضمنية أعلاه.
- عند عدم فهم أي شيء (مثل مسار مفقود، مفتاح صفحة)، **اسأل المستخدم** قبل التنفيذ.
- الزمن للكتب الممسوحة كبير (ساعه+) — أبلغ قبل البدء.

---
راجع سجلات الجلسات السابقة للتحقق:
- **كيف تتقن النحو**: اكتمل 553/553 صفحة عبر Gemini Vision (نص مشكّل، صفر فاشلة) ← في مجلد الكتاب القارّ، والنسخة المشوهة القديمة محفوظة في `_backup_before_gemini/`.
- **كيف تتقن البلاغة**: 617 صفحة (JPEG2000 بطبقة نصية سابقة) — لم يُعالَج بالرؤية الذكية بعد.
- **كيف تتقن الصرف**: 617 صفحة + صفحات قصيرة في نهايتها — لم يُعالَج بالرؤية الذكية بعد.
- **المقرر/الكتب ذات الطبقة النصية** → استخراج مباشر عبر `extract-text.mjs`.